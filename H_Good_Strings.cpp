#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define fast ios_base::sync_with_stdio(false), cin.tie(nullptr), cout.tie(nullptr);
int32_t main(){
    ll k;
    string s,k;
    cin>>k>>s;
    if(s.length()<k)    cout<<"NO"<<endl;
    else{
        k=s[0];
        for(ll i=0;i<s.length();i++){
            if(s[i]!=k) 
        }
    }
return 0;
}
